# Frontend environment variables (Create React App)
# Copy to .env or .env.local and adjust per environment.
# Note: In production builds, CRA proxy is ignored—set API base URL here.

# Backend API base URL
REACT_APP_API_URL=http://localhost:8080

# Optional: environment label for runtime checks (not used by CRA build system)
# REACT_APP_ENV=development
